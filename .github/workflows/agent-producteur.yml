name: 🏭 Agent Producteur (Process Improvement)

# Agent qui détecte automatiquement les problèmes de processus
# Toutes les 12 heures

on:
  schedule:
    # Toutes les 12 heures
    - cron: '0 */12 * * *'
  workflow_dispatch:

jobs:
  producteur:
    name: 🏭 Agent Producteur
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Exécuter Agent Producteur AI
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          echo "🏭 AGENT PRODUCTEUR AI-POWERED - Process Improvement"
          echo "===================================================="
          echo ""

          if [ -z "$ANTHROPIC_API_KEY" ]; then
            echo "⚠️  ANTHROPIC_API_KEY non configuré - Mode fallback"
            echo "ℹ️  Voir CONFIGURATION-IA.md pour activer l'IA réelle"
            echo ""
          else
            echo "✅ Claude AI activé - Détection intelligente des problèmes"
            echo ""
          fi

          chmod +x .github/scripts/autonomous-agents/agent-producteur-ai.js
          node .github/scripts/autonomous-agents/agent-producteur-ai.js

      - name: Upload rapports
        uses: actions/upload-artifact@v4
        with:
          name: producteur-reports
          path: |
            RAPPORT-AGENT-PRODUCTEUR-AI.md
            .github/agents-communication/recommendations.json

      - name: Commit rapports
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          git config user.name "Agent Producteur AI"
          git config user.email "noreply@producteur.bot"

          if [ -n "$(git status --porcelain)" ]; then
            git add RAPPORT-AGENT-PRODUCTEUR-AI.md .github/agents-communication/
            git commit -m "docs: Rapport Agent Producteur AI - Process gaps $([ -z "$ANTHROPIC_API_KEY" ] && echo "(fallback)" || echo "détectés par IA") [skip ci]

            🤖 Generated with [Claude Code](https://claude.com/claude-code)

            Co-Authored-By: Claude <noreply@anthropic.com>"
            git push origin main || echo "OK"
          fi
