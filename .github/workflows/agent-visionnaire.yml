name: 🚀 Agent Visionnaire (6h)

# Agent Visionnaire - Mode Elon Musk
# Propose des innovations et communique avec les autres agents
# Toutes les 6 heures

on:
  schedule:
    # Toutes les 6 heures
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  visionnaire:
    name: 🚀 Agent Visionnaire
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Exécuter Agent Visionnaire AI
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          echo "🚀 AGENT VISIONNAIRE AI-POWERED - Mode Elon Musk"
          echo "================================================"
          echo ""

          if [ -z "$ANTHROPIC_API_KEY" ]; then
            echo "⚠️  ANTHROPIC_API_KEY non configuré - Mode fallback"
            echo "ℹ️  Voir CONFIGURATION-IA.md pour activer l'IA réelle"
            echo ""
          else
            echo "✅ Claude AI activé - Vraies idées innovantes"
            echo ""
          fi

          chmod +x .github/scripts/autonomous-agents/agent-visionnaire-ai.js
          node .github/scripts/autonomous-agents/agent-visionnaire-ai.js

      - name: Upload rapports et recommandations
        uses: actions/upload-artifact@v3
        with:
          name: visionnaire-outputs
          path: |
            RAPPORT-AGENT-VISIONNAIRE-AI.md
            .github/agents-communication/recommendations.json

      - name: Commit rapports et recommandations
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          git config user.name "Agent Visionnaire AI"
          git config user.email "noreply@visionnaire.bot"

          if [ -n "$(git status --porcelain)" ]; then
            git add RAPPORT-AGENT-VISIONNAIRE-AI.md .github/agents-communication/
            git commit -m "docs: Rapport Agent Visionnaire AI + Recommandations [skip ci]

            🚀 Nouvelles innovations et opportunités $([ -z "$ANTHROPIC_API_KEY" ] && echo "(mode fallback)" || echo "détectées par IA")

            🤖 Generated with [Claude Code](https://claude.com/claude-code)

            Co-Authored-By: Claude <noreply@anthropic.com>"

            git push origin main || echo "OK"

            echo "✅ Recommandations communiquées aux autres agents"
          fi
