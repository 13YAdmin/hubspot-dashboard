name: 🚀 Agent Visionnaire (6h)

# Agent Visionnaire - Mode Elon Musk
# Propose des innovations et communique avec les autres agents
# Toutes les 6 heures

on:
  schedule:
    # Toutes les 6 heures
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  visionnaire:
    name: 🚀 Agent Visionnaire
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Exécuter Agent Visionnaire
        run: |
          echo "🚀 AGENT VISIONNAIRE - Mode Elon Musk"
          echo "================================================"
          echo ""

          chmod +x .github/scripts/autonomous-agents/agent-visionnaire.js
          node .github/scripts/autonomous-agents/agent-visionnaire.js

      - name: Upload rapports et recommandations
        uses: actions/upload-artifact@v3
        with:
          name: visionnaire-outputs
          path: |
            RAPPORT-AGENT-VISIONNAIRE.md
            .github/agents-communication/recommendations.json

      - name: Commit rapports et recommandations
        run: |
          git config user.name "Agent Visionnaire"
          git config user.email "noreply@visionnaire.bot"

          if [ -n "$(git status --porcelain)" ]; then
            git add RAPPORT-AGENT-VISIONNAIRE.md .github/agents-communication/
            git commit -m "docs: Rapport Agent Visionnaire + Recommandations [skip ci]

            🚀 Nouvelles innovations et opportunités détectées

            🤖 Generated with [Claude Code](https://claude.com/claude-code)

            Co-Authored-By: Claude <noreply@anthropic.com>"

            git push origin main || echo "OK"

            echo "✅ Recommandations communiquées aux autres agents"
          fi
