name: Deploy Dashboard Only

# Déploie UNIQUEMENT le dashboard (public/index.html) sans refetch des données
# Utile pour déployer des fixes frontend rapides

on:
  workflow_dispatch:  # Manuel uniquement

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
          publish_branch: gh-pages
          commit_message: '🚀 Deploy dashboard fix [skip ci]'
