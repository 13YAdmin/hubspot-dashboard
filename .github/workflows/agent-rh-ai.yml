name: 👔 Agent RH AI (Recruitment)

# Agent RH AI-Powered - Gestion intelligente du recrutement
# Toutes les 12 heures

on:
  schedule:
    # Toutes les 12 heures
    - cron: '30 */12 * * *'
  workflow_dispatch:

jobs:
  rh-ai:
    name: 👔 Agent RH AI
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Exécuter Agent RH AI
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          echo "👔 AGENT RH AI-POWERED - Recruitment Manager"
          echo "=============================================="
          echo ""

          if [ -z "$ANTHROPIC_API_KEY" ]; then
            echo "⚠️  ANTHROPIC_API_KEY non configuré - Mode fallback"
            echo "ℹ️  Voir CONFIGURATION-IA.md pour activer l'IA réelle"
            echo ""
          else
            echo "✅ Claude AI activé - Décisions intelligentes de recrutement"
            echo ""
          fi

          chmod +x .github/scripts/autonomous-agents/agent-rh-ai.js
          node .github/scripts/autonomous-agents/agent-rh-ai.js

      - name: Upload rapports
        uses: actions/upload-artifact@v4
        with:
          name: rh-reports
          path: |
            RAPPORT-AGENT-RH-AI.md
            .github/agents-communication/recommendations.json

      - name: Commit rapports et recommandations
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          git config user.name "Agent RH AI"
          git config user.email "noreply@rh.bot"

          if [ -n "$(git status --porcelain)" ]; then
            git add RAPPORT-AGENT-RH-AI.md .github/agents-communication/
            git commit -m "docs: Rapport Agent RH AI - Besoins recrutement $([ -z "$ANTHROPIC_API_KEY" ] && echo "(fallback)" || echo "analysés par IA") [skip ci]

            👔 Analyse workforce planning et recommandations recrutement

            🤖 Generated with [Claude Code](https://claude.com/claude-code)

            Co-Authored-By: Claude <noreply@anthropic.com>"
            git push origin main || echo "OK"

            echo "✅ Recommandations de recrutement communiquées au Chef"
          fi
